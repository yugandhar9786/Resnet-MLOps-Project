apiVersion: v1
kind: Service
metadata:
  name: resnet-mlops-service
spec:
  type: NodePort
  selector:
    app: resnet-mlops
  ports:
    - port: 80
      targetPort: 8000
      nodePort: 30007
      
